syntax = "proto3";
package proto;
option go_package = "cli.lilmod.dev/proto;proto";

service API {
    rpc Proxy(ProxyRequest) returns (ProxyResponse);
}

message ProxyRequest {
    string proxyToken = 1;
}

message ProxyResponse {
    string URL = 1;
    repeated string privatePaths = 2;
}
